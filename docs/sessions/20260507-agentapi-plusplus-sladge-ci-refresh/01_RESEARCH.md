# Research

## Repository State

- Canonical agentapi-plusplus is clean on `ci/add-golangci-lint` at `f2715fe`.
- Older prepared branch `docs/agentapi-plusplus-sladge-current` diverged from
  current head by `4 1`, so commit `032364f` is stale evidence for the active
  branch.
- Repo-local agent instructions require `task` targets for build, lint, and
  test validation where practical.

## Existing Badge State

The active branch README did not contain `sladge.net` or `AI Slop Inside`
before this refresh.
